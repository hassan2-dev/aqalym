/**
 * Optional helper to seed Firestore from demo data.
 * Run after configuring Firebase credentials (not for Expo Go demo mode).
 *
 * Usage (Node / admin script): import seed data and write to collections.
 */
export {
  SEED_ACCESSORIES,
  SEED_CATALOGS,
  SEED_CATEGORIES,
  SEED_GLASS_TYPES,
  SEED_PRODUCTS,
  SEED_PROJECTS,
} from '@/data/seed';
